@extends('admin.layout.main')@section('content')    <section class="content">        <div class="row">            <div class="col-lg-12">                <div class="box box-primary">                    <div class="box-header with-border">                        <h3 class="box-title"><span>{{ $environments[$environment] }}</span>&nbsp;<b>{{ $file }}</b></h3>                    </div>                    <form class="form-horizontal" role="form" method="post" action="{{ $controller->getRoute('save') }}">                        <input type="hidden" name="language" value="{{ $language }}" />                        <input type="hidden" name="environment" value="{{ $environment }}" />                        <input type="hidden" name="file" value="{{ $file }}" />                        {!! csrf_field() !!}                        <div class="box-body">                            @foreach($data as $key=>$value)                                @if(is_array($value))                                    @foreach($value as $subkey=>$subvalue)                                        @if(is_string($subvalue))                                    <div class="form-group">                                        <label class="col-lg-2 control-label" for="{{ $subkey }}">{{ $subkey }}</label>                                        <div class="col-lg-9">                                            <input type="text" class="form-control" name="trans[{{ $key }}][{{ $subkey }}]" id="{{ $subkey }}" value="{{ $subvalue }}"/>                                        </div>                                    </div>                                        @endif                                    @endforeach                                @else                                <div class="form-group">                                    <label class="col-lg-2 control-label" for="{{ $key }}">{{ $key }}</label>                                    <div class="col-lg-9">                                        <input type="text" class="form-control" name="trans[{{ $key }}]" id="{{ $key }}" value="{{ $value }}"/>                                    </div>                                </div>                                @endif                            @endforeach                        </div>                        <div  class="box-footer ">                            <button type="submit" class="btn btn-primary pull-right">{{ transTpl('save') }}</button>                        </div>                    </form>                </div>            </div>        </div>    </section>@endsection